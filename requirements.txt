# Core library, demo and tests run on the Python standard library alone.
# There is nothing to install to run `python demo.py`, the pipelines' --dry-run
# paths, or the pure-Python test suite.
#
#   Serving layer .............. pip install -r requirements-serve.txt
#   Real model / train / export  pip install -r requirements-ml.txt
#   Dev tooling (lint, test) ... pip install -r requirements-dev.txt
